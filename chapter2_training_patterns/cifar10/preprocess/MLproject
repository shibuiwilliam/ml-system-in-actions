name: cifar10_initial

docker_env:
  image: shibui/ml-system-in-actions:training_pattern_cifar10_initial_0.0.1
  volumes: ["$(pwd)/data:/opt/data"]

entry_points:
  preprocess:
    parameters:
      data: {type: string, default: cifar10}
      downstream: {type: string, default: /opt/data/preprocess/}
    command: |
      python -m src.preprocess \
        --data {data} \
        --downstream {downstream}